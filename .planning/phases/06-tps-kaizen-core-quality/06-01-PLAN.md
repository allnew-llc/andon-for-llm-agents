---
phase: 06-tps-kaizen-core-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/tps-kaizen/SKILL.md
autonomous: true
requirements:
  - SKILL-01
  - SKILL-02
  - SKILL-07

must_haves:
  truths:
    - "Claude Code model invokes tps-kaizen when user describes an error, failure, incident, broken system, stuck pipeline, or regression"
    - "A first-time reader of tps-kaizen SKILL.md finds a Gotchas section with 5+ named failure patterns and can avoid common mistakes"
    - "tps-kaizen SKILL.md contains a related_skills section linking to pipeline-debugging, adversarial-review, and qc-audit with brief descriptions of when to chain"
  artifacts:
    - path: "skills/tps-kaizen/SKILL.md"
      provides: "Trigger-focused description, Gotchas section, related_skills section"
      contains: "error|failure|incident|broken|stuck|regression"
  key_links:
    - from: "skills/tps-kaizen/SKILL.md (description field)"
      to: "Claude Code skill invocation logic"
      via: "frontmatter description keyword matching"
      pattern: "error|failure|incident|broken|stuck|regression"
    - from: "skills/tps-kaizen/SKILL.md (related_skills)"
      to: "pipeline-debugging, adversarial-review, qc-audit"
      via: "explicit skill path references"
      pattern: "related_skills"
---

<objective>
Upgrade tps-kaizen SKILL.md with a trigger-focused description, documented failure patterns (Gotchas), and skill composition links (related_skills).

Purpose: The current description ("Lean manufacturing based anomaly detection...") is too abstract for Claude Code's skill invocation logic to match user problems to this skill. Users also lack documentation of common failure patterns and how to compose tps-kaizen with other skills.

Output: Updated `skills/tps-kaizen/SKILL.md` with three new sections that satisfy SKILL-01, SKILL-02, and SKILL-07.
</objective>

<execution_context>
@/Users/masa/.config/claude/get-shit-done/workflows/execute-plan.md
@/Users/masa/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/tps-kaizen/SKILL.md
@rules/70-kaizen-learning.md
@rules/45-quality-driven-execution.md
@hooks/tps-kaizen-runtime.py

<interfaces>
<!-- Current SKILL.md frontmatter that must be updated: -->
```yaml
---
name: TPS Kaizen
description: Lean manufacturing (Jidoka + JIT) based anomaly detection, root cause analysis, and continuous improvement skill
argument-hint: "<subcommand> [args] — andon, five-whys, kaizen, audit"
version: 1.0.0
---
```

<!-- Failure patterns sourced from codebase (for Gotchas section): -->

1. Human Error Stop (from SKILL.md line 135, five-whys-template.md line 152):
   Five Whys must not stop at "human error" — dig into process/system issues

2. 4-Artifact Close (from PROJECT.md JIDOKA-03, tps-kaizen.md line 83):
   ANDON close requires evidence.json, analysis.json, actions.json, report.md

3. Meta-ANDON Session Reset (from hooks/meta_andon_guard.py, rules/70-kaizen-learning.md):
   Meta-ANDON triggers on repeated failures but counter resets per session

4. Gate-Gaming (from rules/45-quality-driven-execution.md):
   LLMs optimize for Gate conditions instead of actual quality

5. Confidence Threshold (from PROJECT.md JIDOKA-04, hooks/tps-kaizen-runtime.py line 408+):
   Close requires confidence >= 0.70; low confidence needs "manual-approved:" prefix

<!-- Related skills (referenced in roadmap/requirements — some planned, not yet created): -->
- pipeline-debugging: debugging stuck/failed pipelines (planned skill or existing pattern)
- adversarial-review: critical review of own work (planned skill or existing pattern)
- qc-audit: quality self-assessment (Phase 8 deliverable)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md with trigger-focused description, Gotchas, and related_skills</name>
  <files>skills/tps-kaizen/SKILL.md</files>
  <action>
Modify the existing `skills/tps-kaizen/SKILL.md` with three targeted changes. Preserve ALL existing content (Two Pillars, Usage, Subcommands, Glossary, Core Principles sections). Do NOT delete or reorganize existing sections.

**Change 1 — Trigger-focused description (SKILL-01):**

Replace the frontmatter `description` field with a trigger-focused description that lists WHEN to call this skill, not WHAT it is. The new description must include these trigger keywords: error, failure, incident, broken, stuck, regression.

New description format:
```yaml
description: "Use when: error, test failure, build failure, incident, broken pipeline, stuck process, regression, anomaly, or any quality defect. Provides Jidoka (stop-and-fix) response, Five Whys root cause analysis, and Kaizen continuous improvement."
```

Also add a new `## When to Use This Skill` section immediately after the opening quote block (before `## Two Pillars`). This section lists concrete trigger scenarios:

```markdown
## When to Use This Skill

Invoke `/tps-kaizen` when you encounter:

| Trigger | Example | Subcommand |
|---------|---------|------------|
| Test failure | `pytest` returns non-zero, assertion errors | `andon` |
| Build error | Compilation failure, type errors, missing imports | `andon` |
| Runtime exception | Unhandled error in production or dev server | `andon` |
| Pipeline stuck | CI/CD hangs, deployment blocked, merge conflict loop | `andon` |
| Regression | Feature that worked before now fails | `andon` then `five-whys` |
| Incident | User-reported bug, data corruption, service outage | `andon` |
| Unexplained behavior | "It works on my machine", flaky tests | `five-whys` |
| Process inefficiency | Repeated manual work, slow feedback loops | `kaizen` or `audit` |
| Quality degradation | Increasing defect rate, declining test coverage | `audit` |

**Key signals in user messages**: "error", "failure", "broken", "stuck", "regression", "incident", "not working", "keeps failing", "can't figure out why"
```

**Change 2 — Gotchas section (SKILL-02):**

Add a `## Gotchas` section after the `## Core Principles` section (at the end of the file, before any trailing `---`). Document exactly these 5+ named failure patterns:

1. **Human Error Stop** — Five Whys analysis terminates at "human error" or "I made a mistake" instead of digging into the process/system gap that allowed the mistake. Always ask "Why did the process allow this error?" to reach a structural root cause.

2. **4-Artifact Close Shortcut** — Attempting to close ANDON without all 4 artifacts (evidence.json, analysis.json, actions.json, report.md). The close command will reject if any artifact is missing. Do not create placeholder/empty artifacts just to pass validation — each must contain genuine analysis.

3. **Meta-ANDON Session Reset** — Meta-ANDON failure counters reset at session boundaries. A pattern of 2 failures per session across multiple sessions will never trigger Meta-ANDON (requires 3 in one session). Long-running investigations should stay in one session or explicitly note the cross-session pattern.

4. **Gate-Gaming** — LLM agents optimize for Gate pass conditions (required_files, output_markers) instead of genuine quality. This produces thin artifacts that pass validation but fail in practice. Prevention: focus on deliverable quality criteria, not Gate conditions. See `rules/45-quality-driven-execution.md`.

5. **Confidence Threshold Bypass** — ANDON close with root cause confidence below 0.70 is rejected unless the close reason includes `manual-approved:` prefix. Do not inflate confidence scores to bypass the threshold. If genuine confidence is low, get human verification and use `manual-approved: <verified reason>`.

6. **Andon Cord Fear** — Team members hesitate to pull the Andon cord (report problems) because they fear blame or disruption. The TPS principle is: pulling the cord is always encouraged. The cost of stopping is always less than the cost of passing defects downstream.

7. **Fix-and-Forget** — Emergency fix restores normal operation, but Step 4 (Investigate) is skipped. The root cause remains, and the same failure recurs. Always complete the full 4-step Jidoka cycle.

**Change 3 — related_skills section (SKILL-07):**

Add a `## Related Skills` section after the `## Gotchas` section. Include direct links and describe when to chain:

```markdown
## Related Skills

| Skill | Path | When to Chain |
|-------|------|---------------|
| pipeline-debugging | `skills/pipeline-debugging/SKILL.md` | When ANDON is triggered by a pipeline failure — use pipeline-debugging for environment/config diagnosis before Five Whys for root cause |
| adversarial-review | `skills/adversarial-review/SKILL.md` | After kaizen improvements — use adversarial-review to stress-test the fix and verify it handles edge cases |
| qc-audit | `skills/qc-audit/SKILL.md` | After closing ANDON — use qc-audit to verify the fix improved overall quality metrics and did not regress other areas |

### Composition Patterns

**Incident Response Chain**: `andon` (stop + emergency fix) -> `five-whys` (root cause) -> `pipeline-debugging` (if infrastructure-related) -> `adversarial-review` (verify fix robustness)

**Quality Improvement Chain**: `audit` (identify waste) -> `kaizen` (plan improvement) -> `qc-audit` (measure impact)
```

Also bump the version in frontmatter from `1.0.0` to `1.1.0` to reflect the skill quality improvements.
  </action>
  <verify>
    <automated>cd /Users/masa/Development/andon-for-llm-agents && grep -c "error\|failure\|incident\|broken\|stuck\|regression" skills/tps-kaizen/SKILL.md | awk '{if ($1 >= 6) print "PASS: trigger keywords present ("$1" matches)"; else print "FAIL: insufficient trigger keywords ("$1" matches)"}' && echo "---" && grep -c "## Gotchas" skills/tps-kaizen/SKILL.md | awk '{if ($1 >= 1) print "PASS: Gotchas section exists"; else print "FAIL: Gotchas section missing"}' && echo "---" && grep -c "Human Error Stop\|4-Artifact Close\|Meta-ANDON Session Reset\|Gate-Gaming\|Confidence Threshold" skills/tps-kaizen/SKILL.md | awk '{if ($1 >= 5) print "PASS: 5+ named failure patterns ("$1" found)"; else print "FAIL: insufficient failure patterns ("$1" found)"}' && echo "---" && grep -c "## Related Skills" skills/tps-kaizen/SKILL.md | awk '{if ($1 >= 1) print "PASS: related_skills section exists"; else print "FAIL: related_skills section missing"}' && echo "---" && grep -c "pipeline-debugging\|adversarial-review\|qc-audit" skills/tps-kaizen/SKILL.md | awk '{if ($1 >= 3) print "PASS: all 3 related skills linked ("$1" matches)"; else print "FAIL: missing related skill links ("$1" matches)"}' && echo "---" && grep -c "## When to Use This Skill" skills/tps-kaizen/SKILL.md | awk '{if ($1 >= 1) print "PASS: trigger section exists"; else print "FAIL: trigger section missing"}' && echo "---" && grep "version:" skills/tps-kaizen/SKILL.md</automated>
  </verify>
  <done>
    - SKILL.md frontmatter description contains trigger keywords (error, failure, incident, broken, stuck, regression)
    - "When to Use This Skill" section exists with trigger table mapping user problems to subcommands
    - "Gotchas" section exists with 7 named failure patterns (5 required + 2 bonus)
    - "Related Skills" section exists with links to pipeline-debugging, adversarial-review, qc-audit
    - Related Skills includes composition patterns for incident response and quality improvement chains
    - All existing SKILL.md content (Two Pillars, Usage, Subcommands, Glossary, Core Principles) is preserved
    - Version bumped to 1.1.0
  </done>
</task>

</tasks>

<verification>
Phase 6 overall verification:

1. **SKILL-01 check**: `grep -E "error|failure|incident|broken|stuck|regression" skills/tps-kaizen/SKILL.md` returns 6+ matches across the description and trigger section
2. **SKILL-02 check**: `grep -c "###.*Human Error Stop\|###.*4-Artifact\|###.*Meta-ANDON\|###.*Gate-Gaming\|###.*Confidence Threshold" skills/tps-kaizen/SKILL.md` returns >= 5
3. **SKILL-07 check**: `grep -c "pipeline-debugging\|adversarial-review\|qc-audit" skills/tps-kaizen/SKILL.md` returns >= 3, and a "## Related Skills" header exists
4. **No regression**: Existing sections (Two Pillars, Usage, Subcommands, Glossary, Core Principles) still present — verify with `grep -c "## Two Pillars\|## Usage\|## Subcommand\|## TPS Glossary\|## Core Principles" skills/tps-kaizen/SKILL.md` returning >= 5
</verification>

<success_criteria>
- tps-kaizen SKILL.md description is trigger-focused with concrete keywords that help Claude match user problems to this skill
- Gotchas section documents 5+ named failure patterns with actionable prevention guidance
- Related Skills section links to pipeline-debugging, adversarial-review, and qc-audit with composition patterns
- All existing skill content is preserved (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/06-tps-kaizen-core-quality/06-01-SUMMARY.md`
</output>
