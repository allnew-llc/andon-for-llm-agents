---
phase: 14-analysis-metrics-skill-integration
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/tps-kaizen/SKILL.md
autonomous: true
requirements: [INTEG-01, INTEG-02]

must_haves:
  truths:
    - "Running /tps-kaizen shows gotcha-review as a documented subcommand with usage description"
    - "The tps-kaizen andon Step 0 includes a Gotcha Registry check alongside existing incident history check"
  artifacts:
    - path: "skills/tps-kaizen/SKILL.md"
      provides: "Complete tps-kaizen skill documentation with gotcha-review and Step 0 Gotcha Registry check"
      contains: "gotcha-review"
  key_links:
    - from: "skills/tps-kaizen/SKILL.md Step 0"
      to: "gotchas/*.yaml"
      via: "instruction text referencing Gotcha Registry check"
      pattern: "Gotcha Registry"
    - from: "skills/tps-kaizen/SKILL.md gotcha-review"
      to: "gotchas/candidates/"
      via: "documented subcommand procedure"
      pattern: "gotcha-review"
---

<objective>
Verify INTEG-01 (gotcha-review already documented) and implement INTEG-02 (update andon Step 0 to include Gotcha Registry check alongside existing incident history check).

Purpose: Phase 13 already added gotcha-review documentation to SKILL.md, so INTEG-01 should be a verification pass. INTEG-02 requires updating Step 0 so that when ANDON opens, the Claude agent checks both incident history AND the Gotcha Registry for relevant known patterns. Also add gotcha-stats.sh to the tips/references section.

Output: Updated `skills/tps-kaizen/SKILL.md` with Step 0 Gotcha Registry check.
</objective>

<execution_context>
@/Users/masa/.config/claude/get-shit-done/workflows/execute-plan.md
@/Users/masa/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-five-whys-gotcha-loop/13-02-SUMMARY.md

<interfaces>
<!-- Current SKILL.md Step 0 text (lines 96-111) that needs updating -->

```markdown
### Step 0: Check Incident History

Before starting analysis, check for recurring patterns in past incidents.

1. **Scan persistent store**: Check `${CLAUDE_PLUGIN_DATA}/kaizen/incidents/` ...
2. **Match current problem**: Compare the current error message ...
3. **Surface matches**: If matching incidents are found, report them: ...
4. **Inform investigation**: Use past incident data to skip already-explored root causes ...

> **Tip**: Run `skills/tps-kaizen/scripts/aggregate-incidents.sh` to see a full pattern summary before starting.
```

<!-- Current gotcha-review section (lines 280-318) — already added by Phase 13 Plan 02 -->

```markdown
## Subcommand: `gotcha-review`

**Trigger**: After Five Whys generates one or more Gotcha candidates...

### Procedure

1. **List candidates**: Read all `*.yaml` files in `gotchas/candidates/`...
2. **Review each candidate**: For each candidate, ask the user...
3. **Report results**: After reviewing all candidates...
```

<!-- Usage table entry already present (line 85) -->
```
/tps-kaizen gotcha-review          Review and promote Gotcha candidates
```

From hooks/gotcha_surfacer.py (the engine that auto-surfaces during ANDON):
```python
def surface_gotchas(error_text, gotchas_dir, packs_dir=None) -> str
# Auto-called by tps-kaizen-runtime.py during ANDON open
# Matches error text against Gotcha registry patterns
# Returns formatted string with [GOTCHA_MATCH] header
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify INTEG-01 and update Step 0 for INTEG-02</name>
  <files>skills/tps-kaizen/SKILL.md</files>
  <action>
**Part A: Verify INTEG-01 (gotcha-review documentation)**

Run verification checks to confirm Phase 13 already satisfied INTEG-01:
- `grep -c "gotcha-review" skills/tps-kaizen/SKILL.md` should return 4+
- `grep "## Subcommand: .gotcha-review." skills/tps-kaizen/SKILL.md` should find the section
- The Usage table should show `gotcha-review` with a description

If all checks pass, INTEG-01 is confirmed satisfied. Log confirmation in the summary.

If any check fails (unexpected), add the missing content following the pattern established by Phase 13 Plan 02.

**Part B: Implement INTEG-02 (Step 0 Gotcha Registry check)**

Update the `### Step 0: Check Incident History` section in SKILL.md. The current title is "Check Incident History" — rename it to "Check Incident History and Gotcha Registry" to reflect the expanded scope.

Add a step 1.5 (renumber as needed) between the existing "Scan persistent store" step and "Match current problem" step. The new step should instruct Claude to:

1. Keep existing step 1 (Scan persistent store for incidents) as-is
2. **NEW step 2**: **Check Gotcha Registry**: Load Gotchas from `gotchas/` directory (and `packs/*/gotchas/` if present). The ANDON hook automatically surfaces matching Gotchas via `gotcha_surfacer.py` — check the hook output for `[GOTCHA_MATCH]` markers. If Gotcha matches were surfaced, note them:
   ```
   [Gotcha] Found N known anti-pattern(s) matching this incident:
   - GOTCHA-XXX: [name] — [prevention advice]
   Use matched Gotcha prevention advice to guide investigation.
   ```
3. Renumber subsequent steps (old step 2 becomes step 3, old step 3 becomes step 4, old step 4 becomes step 5)
4. Update the investigation guidance (old step 4, now step 5) to mention: "Use both past incident data AND matched Gotcha prevention advice to focus the investigation"

Also update the Tip at the bottom of Step 0 to mention gotcha-stats.sh:
```
> **Tip**: Run `skills/tps-kaizen/scripts/aggregate-incidents.sh` for incident pattern summary, and `skills/tps-kaizen/scripts/gotcha-stats.sh` for Gotcha hit rates and effectiveness.
```

Do NOT change the version number (it stays at 1.2.0 — this is a documentation refinement within the same feature set, not a new feature).

Do NOT modify any other sections of SKILL.md. Make surgical edits to Step 0 only (plus the Tip line).
  </action>
  <verify>
    <automated>grep -c "Gotcha Registry" skills/tps-kaizen/SKILL.md && grep -c "gotcha-stats.sh" skills/tps-kaizen/SKILL.md && grep -c "GOTCHA_MATCH" skills/tps-kaizen/SKILL.md && grep -c "gotcha-review" skills/tps-kaizen/SKILL.md</automated>
  </verify>
  <done>
    - INTEG-01 verified: gotcha-review appears in Usage table and has a dedicated ## Subcommand section
    - INTEG-02 implemented: Step 0 title includes "Gotcha Registry", new step instructs checking GOTCHA_MATCH output from ANDON hook
    - Step 0 Tip references both aggregate-incidents.sh and gotcha-stats.sh
    - No other sections of SKILL.md were modified
    - grep "Gotcha Registry" returns at least 1 match
    - grep "GOTCHA_MATCH" returns at least 1 match in Step 0 context
  </done>
</task>

</tasks>

<verification>
1. `grep "gotcha-review" skills/tps-kaizen/SKILL.md` — appears in Usage table + dedicated section (INTEG-01)
2. `grep "Gotcha Registry" skills/tps-kaizen/SKILL.md` — appears in Step 0 heading (INTEG-02)
3. `grep "GOTCHA_MATCH" skills/tps-kaizen/SKILL.md` — referenced in Step 0 instructions
4. `grep "gotcha-stats.sh" skills/tps-kaizen/SKILL.md` — referenced in Step 0 Tip
5. `grep "version:" skills/tps-kaizen/SKILL.md` — still shows 1.2.0
</verification>

<success_criteria>
- gotcha-review is documented as a subcommand with usage description in SKILL.md (INTEG-01 verified)
- andon Step 0 includes Gotcha Registry check alongside incident history check (INTEG-02 implemented)
- Step 0 Tip references gotcha-stats.sh for effectiveness metrics
</success_criteria>

<output>
After completion, create `.planning/phases/14-analysis-metrics-skill-integration/14-02-SUMMARY.md`
</output>
