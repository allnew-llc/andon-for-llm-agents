---
phase: 13-five-whys-gotcha-loop
plan: "02"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - skills/tps-kaizen/SKILL.md
autonomous: true
requirements: [LOOP-03, LOOP-04]

must_haves:
  truths:
    - "Running /tps-kaizen gotcha-review lists all YAML files in gotchas/candidates/ with id, pattern, and severity fields"
    - "Approving a candidate in gotcha-review moves it from gotchas/candidates/ to gotchas/ without manual file operations"
    - "A promoted Gotcha is immediately available to load_gotchas() because it reads from disk on each call"
    - "Five Whys Step 4 (Investigate) includes a post-step to run gotcha candidate generation after prevention measures are defined"
  artifacts:
    - path: "skills/tps-kaizen/SKILL.md"
      provides: "gotcha-review subcommand section and Five Whys gotcha candidate integration"
      contains: "gotcha-review"
  key_links:
    - from: "skills/tps-kaizen/SKILL.md (gotcha-review)"
      to: "gotchas/candidates/"
      via: "Instruction to list *.yaml files in candidates/"
      pattern: "gotchas/candidates"
    - from: "skills/tps-kaizen/SKILL.md (gotcha-review)"
      to: "gotchas/"
      via: "Instruction to mv approved file from candidates/ to gotchas/"
      pattern: "mv.*candidates.*gotchas"
    - from: "skills/tps-kaizen/SKILL.md (five-whys)"
      to: "hooks/gotcha_candidate.py"
      via: "Instruction to call generate_candidate() after Five Whys prevention measures"
      pattern: "generate_candidate"
    - from: "gotchas/ (promoted file)"
      to: "hooks/gotcha_registry.py load_gotchas()"
      via: "load_gotchas() reads *.yaml from disk on every call"
      pattern: "load_gotchas.*gotchas_dir"
---

<objective>
Add the gotcha-review subcommand to tps-kaizen SKILL.md and integrate gotcha candidate generation into the Five Whys completion flow.

Purpose: This completes the Five Whys -> Gotcha learning loop. After Five Whys identifies a root cause and prevention, the system generates a candidate. The gotcha-review subcommand lets users approve candidates into the live registry. Since load_gotchas() reads from disk each call, promoted Gotchas are immediately available.

Output: Updated skills/tps-kaizen/SKILL.md with gotcha-review subcommand section and Five Whys Step 4 integration.
</objective>

<execution_context>
@/Users/masa/.config/claude/get-shit-done/workflows/execute-plan.md
@/Users/masa/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-five-whys-gotcha-loop/13-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 01 that this plan references in SKILL.md instructions -->

From hooks/gotcha_candidate.py (created in Plan 01):
```python
def generate_candidate(
    root_cause: str,
    prevention: str,
    *,
    severity: str = "medium",
    source: str = "five-whys",
    gotchas_dir: Path | None = None,
    tags: list[str] | None = None,
) -> Path:
    """Generate a Gotcha candidate YAML from Five Whys output.
    Returns the Path to the created YAML file in gotchas/candidates/."""
```

From hooks/gotcha_registry.py (existing):
```python
def load_gotchas(gotchas_dir: Path, *, packs_dir: Path | None = None) -> list[GotchaEntry]:
    """Reads *.yaml from disk on every call — no caching.
    A file moved into gotchas_dir is immediately discoverable."""
```

Current SKILL.md Usage section (line ~79-85):
```
/tps-kaizen andon <problem>       Execute Jidoka 4-step anomaly response
/tps-kaizen five-whys <problem>   Root cause analysis via Five Whys
/tps-kaizen kaizen <area>         Run improvement cycle via Improvement Kata
/tps-kaizen audit                 3M (Muda/Mura/Muri) waste audit
/tps-kaizen capture [learning]    Capture and standardize knowledge from fixes
```

Current Five Whys Step 4 (Investigate) in andon section (lines ~146-149):
```
1. Run `/tps-kaizen five-whys` to identify root cause
2. Design prevention measures for the root cause
3. Embed prevention in standard work (rules, templates, CI config, etc.)
4. Add automatic detection (poka-yoke) for the same class of problems
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gotcha-review subcommand and Five Whys candidate generation to SKILL.md</name>
  <files>skills/tps-kaizen/SKILL.md</files>
  <action>
Update skills/tps-kaizen/SKILL.md with three targeted changes:

**Change 1: Add gotcha-review to Usage table (around line 80)**

Add a new line to the usage block:
```
/tps-kaizen gotcha-review          Review and promote Gotcha candidates
```

**Change 2: Add gotcha-review subcommand section**

Insert a new section AFTER the `capture` subcommand section (before the TPS Glossary section). The gotcha-review subcommand is instruction-based (tells Claude what to do), not a script:

```markdown
## Subcommand: `gotcha-review`

**Trigger**: After Five Whys generates one or more Gotcha candidates, or periodically to check pending candidates

Review Gotcha candidates in `gotchas/candidates/` and promote approved ones to the live registry.

### Procedure

1. **List candidates**: Read all `*.yaml` files in `gotchas/candidates/`. For each, display:
   ```
   Candidate: {id}
     Pattern:  {pattern (first 80 chars)}
     Severity: {severity}
     Source:   {source}
   ```
   If no candidates exist, report "No Gotcha candidates pending review." and stop.

2. **Review each candidate**: For each candidate, ask the user:
   ```
   Approve this candidate? [approve / skip / edit]
   ```
   - **approve**: Move the YAML file from `gotchas/candidates/{file}` to `gotchas/{file}` using `mv`. The Gotcha is immediately available to `load_gotchas()` (no restart required because `load_gotchas()` reads from disk on each call).
   - **skip**: Leave the candidate in `gotchas/candidates/` for later review.
   - **edit**: Open the YAML content for inline editing (severity, pattern wording, tags), then re-ask approve/skip.

3. **Report results**: After reviewing all candidates, summarize:
   ```
   Gotcha Review Complete:
   - Approved: N (moved to gotchas/)
   - Skipped: N (remain in candidates/)
   - Total reviewed: N
   ```

### Notes

- Promoted Gotchas are immediately matchable by the ANDON surfacing engine in the same session because `load_gotchas()` reads from disk on every call with no caching.
- The candidate file name is preserved during promotion (the ID is already in the YAML).
- If a promoted Gotcha has an ID that conflicts with an existing Gotcha, `load_gotchas()` will raise `GotchaValidationError` on next load. Resolve by editing the candidate's `id` field before approval.
```

**Change 3: Add gotcha candidate generation to Five Whys flow**

In the `andon` subcommand's Step 4 (Investigate), add a new step 5 after the existing 4 steps (around line 149):

```markdown
5. **Generate Gotcha candidate**: After defining prevention measures, run gotcha candidate generation to capture the learning:
   ```python
   from gotcha_candidate import generate_candidate
   candidate_path = generate_candidate(
       root_cause="<root cause from Five Whys>",
       prevention="<prevention measures defined above>",
       tags=["<category from Five Whys>"]
   )
   ```
   Report the candidate path and suggest running `/tps-kaizen gotcha-review` to promote it.
```

Also add a similar note at the END of the `five-whys` subcommand Output Format section (after the template block, around line 201), before the "Detailed template" reference line:

```markdown
> **After completion**: Run gotcha candidate generation to capture this learning as a reviewable Gotcha:
> ```python
> from gotcha_candidate import generate_candidate
> generate_candidate(root_cause="<root cause>", prevention="<prevention measures>")
> ```
> Then run `/tps-kaizen gotcha-review` to approve the candidate into the live registry.
```

**Change 4: Update version in frontmatter**

Bump version from `1.1.0` to `1.2.0` in the YAML frontmatter.

IMPORTANT: Do NOT rewrite the entire file. Make only these four targeted changes. Preserve all existing content exactly as-is.
  </action>
  <verify>
    <automated>cd /Users/masa/Development/andon-for-llm-agents && grep -c "gotcha-review" skills/tps-kaizen/SKILL.md && grep -c "generate_candidate" skills/tps-kaizen/SKILL.md && grep "version:" skills/tps-kaizen/SKILL.md && grep -c "gotchas/candidates/" skills/tps-kaizen/SKILL.md</automated>
  </verify>
  <done>
    - SKILL.md contains gotcha-review in the Usage table
    - SKILL.md has a complete "Subcommand: gotcha-review" section with list/approve/skip/edit workflow
    - SKILL.md Five Whys Step 4 includes gotcha candidate generation as step 5
    - SKILL.md five-whys Output Format section includes post-completion note about candidate generation
    - Version bumped to 1.2.0
    - gotcha-review instructions reference gotchas/candidates/ directory
    - gotcha-review instructions explain immediate availability after promotion (no restart)
    - All existing SKILL.md content preserved without modification
  </done>
</task>

</tasks>

<verification>
```bash
# gotcha-review subcommand is documented
grep -n "gotcha-review" skills/tps-kaizen/SKILL.md

# Candidate generation is wired into Five Whys flow
grep -n "generate_candidate" skills/tps-kaizen/SKILL.md

# Version was bumped
grep "version:" skills/tps-kaizen/SKILL.md

# Immediate availability is documented (LOOP-04)
grep -n "no restart\|immediately\|no caching" skills/tps-kaizen/SKILL.md

# Promotion workflow mentions mv from candidates to gotchas
grep -n "candidates.*gotchas\|gotchas.*candidates" skills/tps-kaizen/SKILL.md
```
</verification>

<success_criteria>
- /tps-kaizen gotcha-review is a documented subcommand in SKILL.md with list/approve/skip/edit workflow
- Five Whys completion flow includes gotcha candidate generation as a post-step
- Promotion is documented as mv from gotchas/candidates/ to gotchas/
- Immediate availability after promotion is explicitly documented (load_gotchas reads disk, no cache)
- SKILL.md version bumped to 1.2.0
</success_criteria>

<output>
After completion, create `.planning/phases/13-five-whys-gotcha-loop/13-02-SUMMARY.md`
</output>
