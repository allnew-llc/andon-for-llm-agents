---
phase: 09-standalone-skill-upgrades
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/skills/ios-app-factory-operator/SKILL.md
  - ~/.claude/skills/blog-reader-critic/SKILL.md
  - ~/.claude/skills/blog-reader-critic/references/audit-protocol.md
autonomous: true
requirements: [UPGRADE-03, UPGRADE-04]

must_haves:
  truths:
    - "ios-app-factory-operator/ directory exists with SKILL.md that has trigger-focused description telling Claude when to invoke it"
    - "blog-reader-critic/ directory exists with SKILL.md that has trigger-focused description and progressive disclosure via references/"
    - "The old flat ios-app-factory-operator.md and blog-reader-critic.md files are deleted"
  artifacts:
    - path: "~/.claude/skills/ios-app-factory-operator/SKILL.md"
      provides: "Operator guide for ios-app-factory pipeline with trigger-focused description"
      contains: "Use when"
    - path: "~/.claude/skills/blog-reader-critic/SKILL.md"
      provides: "Hostile reviewer persona skill with trigger-focused description"
      contains: "Gotchas"
    - path: "~/.claude/skills/blog-reader-critic/references/audit-protocol.md"
      provides: "Detailed audit protocol and evaluation criteria for progressive disclosure"
  key_links:
    - from: "~/.claude/skills/blog-reader-critic/SKILL.md"
      to: "~/.claude/skills/blog-reader-critic/references/audit-protocol.md"
      via: "Additional Resources section"
      pattern: "references/audit-protocol.md"
---

<objective>
Upgrade ios-app-factory-operator.md and blog-reader-critic.md to SKILL.md directory structure with trigger-focused descriptions and progressive disclosure.

Purpose: These two skills currently lack YAML frontmatter entirely, making them invisible to Claude Code's skill invocation matching. The upgrade adds trigger-focused descriptions so Claude can find them at the right moment.

Output: Two directory-based skills (ios-app-factory-operator/, blog-reader-critic/) replacing two flat .md files.
</objective>

<execution_context>
@/Users/masa/.config/claude/get-shit-done/workflows/execute-plan.md
@/Users/masa/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tps-kaizen-core-quality/06-01-SUMMARY.md

<interfaces>
<!-- Established SKILL.md pattern from Phase 6 -->

SKILL.md structure:
```yaml
---
name: skill-name
description: Trigger-focused description. Use when: <keyword1>, <keyword2>...
---
```
Followed by:
1. # /skill-name heading
2. ## Usage or ## When to Use
3. ## Core content sections
4. ## Gotchas (named patterns with ### headings, where applicable)
5. ## Additional Resources (links to references/ files, where content warrants splitting)

Note: ios-app-factory-operator is relatively compact (59 lines) and may not need references/ splitting.
blog-reader-critic is 99 lines with a substantial audit protocol section suitable for references/ extraction.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade ios-app-factory-operator.md to directory SKILL.md with trigger-focused description</name>
  <files>
    ~/.claude/skills/ios-app-factory-operator/SKILL.md
  </files>
  <action>
Create directory structure:
- mkdir -p ~/.claude/skills/ios-app-factory-operator/

Create ~/.claude/skills/ios-app-factory-operator/SKILL.md:
1. YAML frontmatter with:
   - name: ios-app-factory-operator
   - description: "Operator guide for the ios-app-factory pipeline. Use when: modifying factory.py, debugging pipeline failures, adjusting phase executors, editing scaffold templates, running compliance gates, operating ios-app-factory."
2. Restructure content from the current flat file:
   - # /ios-app-factory-operator heading (convert from current Japanese heading)
   - ## When to Use — convert the current "## 使うタイミング" bullet list into a trigger table (English, matching Phase 6 pattern):

     | Trigger | Action |
     |---------|--------|
     | Modifying factory.py / orchestrator.py / agents/definitions.py | Read this skill first for change policy |
     | Adjusting Phase 0 ideation logic | Check ideation/ directory scope |
     | Modifying MCP tools in tools/ | Verify MCP tool behavior contracts |
     | Editing templates or scaffold_app.py | Preserve scaffold test infrastructure |
     | Pipeline stopped or failing | Use preflight checks to diagnose |

   - ## Preflight Checks — keep the existing bash checks but reframe the section heading from Japanese
   - ## Execution Modes — keep the three modes (manual idea, auto ideation, compliance gate) with bash blocks
   - ## Change Policy — keep the 4 rules from "## 変更時の基本方針" but translate to English for consistency with other SKILL.md files (or keep bilingual if the user's original content was intentionally Japanese — since the file mixes Japanese and English, translate section headers to English but keep the meaning identical)
   - ## Known Issues — convert "## 既知ドリフト（要先確認）" section, noting all 4 items are marked as resolved
   - ## References — keep the 4 reference links to the .codex/skills/ directory (these are external references, not references/ subdirectory files)

Note: This skill is compact enough that it does NOT need a references/ subdirectory. The existing external references to .codex/skills/ paths are adequate. Do not create an empty references/ directory.

After creating the file, delete the old flat file:
```bash
rm ~/.claude/skills/ios-app-factory-operator.md
```

IMPORTANT: The critical change is adding YAML frontmatter with a trigger-focused description. The content restructure is secondary but improves consistency with the established SKILL.md pattern.
  </action>
  <verify>
    <automated>test -f ~/.claude/skills/ios-app-factory-operator/SKILL.md && ! test -f ~/.claude/skills/ios-app-factory-operator.md && grep -q "^---" ~/.claude/skills/ios-app-factory-operator/SKILL.md && grep -q "Use when" ~/.claude/skills/ios-app-factory-operator/SKILL.md && grep -q "name: ios-app-factory-operator" ~/.claude/skills/ios-app-factory-operator/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>ios-app-factory-operator/ directory exists with SKILL.md that has YAML frontmatter (name, trigger-focused description with "Use when:" keywords), restructured content with English section headers, old flat file deleted</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade blog-reader-critic.md to directory SKILL.md with references/</name>
  <files>
    ~/.claude/skills/blog-reader-critic/SKILL.md
    ~/.claude/skills/blog-reader-critic/references/audit-protocol.md
  </files>
  <action>
Create directory structure:
- mkdir -p ~/.claude/skills/blog-reader-critic/references/

Create ~/.claude/skills/blog-reader-critic/SKILL.md:
1. YAML frontmatter with:
   - name: blog-reader-critic
   - description: "Hostile technical reviewer persona for blog post critique. Use when: reviewing blog post, writing technical article, checking blog draft, improving technical writing, pre-publication review."
2. Restructure content — keep the persona definition and core evaluation criteria in SKILL.md, extract detailed audit protocol to references/:
   - # /blog-reader-critic heading
   - Brief persona introduction (the "senior AI/ML platform engineer" paragraph — this is the identity, keep it in SKILL.md)
   - ## Your Background — keep the 5 background bullets (concise, essential for persona establishment)
   - ## Your Reading Behavior — keep the reading behavior section (essential for understanding the persona's mindset)
   - ## Critical Evaluation Criteria — keep the three tables (INSTANT KILL, YELLOW FLAGS, WHAT MAKES YOU KEEP READING) in SKILL.md. These are the core value of the skill and need to be loaded for every invocation.
   - ## Output Format — keep the output template (KEEP/REWRITE/CUT per section, overall rating)
   - ## Gotchas — add a new Gotchas section with named patterns:
     - ### Persona Drift — The reviewer must stay in character as the hostile skeptic throughout. If Claude starts being "helpful" or "constructive" without being asked, it breaks the skill's purpose.
     - ### Confirmation Bias — When reviewing your own writing, the reviewer must NOT have access to your intentions or outline. The whole point is fresh-eyes critique.
     - ### Kindness Creep — "REWRITE" is the right verdict when it's warranted. Do not soften to "SUGGESTION" to be polite — the user wants honest, harsh feedback.
   - ## Additional Resources — point to references/audit-protocol.md

Extract to references/audit-protocol.md:
- ## Audit Protocol section (the 5 named tests: "SO WHAT?", "PROVE IT", "I ALREADY KNEW THAT", "WOULD I SHARE THIS?", "HACKER NEWS COMMENT") — these are supplementary audit steps that provide depth but aren't needed for every invocation. The main evaluation criteria tables are sufficient for most uses.

After creating both files, delete the old flat file:
```bash
rm ~/.claude/skills/blog-reader-critic.md
```
  </action>
  <verify>
    <automated>test -f ~/.claude/skills/blog-reader-critic/SKILL.md && test -f ~/.claude/skills/blog-reader-critic/references/audit-protocol.md && ! test -f ~/.claude/skills/blog-reader-critic.md && grep -q "name: blog-reader-critic" ~/.claude/skills/blog-reader-critic/SKILL.md && grep -q "Gotchas" ~/.claude/skills/blog-reader-critic/SKILL.md && grep -q "references/audit-protocol.md" ~/.claude/skills/blog-reader-critic/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>blog-reader-critic/ directory exists with SKILL.md (trigger-focused description, persona + evaluation criteria retained, 3 named Gotchas, Additional Resources section), references/audit-protocol.md exists with the 5-test audit protocol, old flat file deleted</done>
</task>

</tasks>

<verification>
1. Both ios-app-factory-operator/ and blog-reader-critic/ directories exist under ~/.claude/skills/
2. Each has SKILL.md with YAML frontmatter (name, trigger-focused description)
3. blog-reader-critic has references/ subdirectory with audit-protocol.md
4. ios-app-factory-operator does not have an empty references/ (compact skill, external refs sufficient)
5. Old flat .md files are deleted
6. blog-reader-critic/SKILL.md has Gotchas section with named patterns
</verification>

<success_criteria>
- ios-app-factory-operator/SKILL.md exists with YAML frontmatter and trigger-focused "Use when:" description
- blog-reader-critic/SKILL.md exists with trigger-focused description, Gotchas, and references/audit-protocol.md
- Old flat files deleted
- All functional content preserved
</success_criteria>

<output>
After completion, create `.planning/phases/09-standalone-skill-upgrades/09-02-SUMMARY.md`
</output>
