---
phase: 09-standalone-skill-upgrades
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/skills/freeze/SKILL.md
  - ~/.claude/skills/freeze/references/hook-setup.md
  - ~/.claude/skills/cherry-pick-prod/SKILL.md
  - ~/.claude/skills/cherry-pick-prod/references/pr-template.md
autonomous: true
requirements: [UPGRADE-01, UPGRADE-02]

must_haves:
  truths:
    - "freeze/ directory exists with SKILL.md that has trigger-focused description and on-demand hook registration pattern"
    - "freeze/ SKILL.md has a Gotchas section with named failure patterns including session restart requirement and symlink resolution"
    - "cherry-pick-prod/ directory exists with SKILL.md that has trigger-focused description"
    - "cherry-pick-prod/ SKILL.md has a Gotchas section and a references/ subdirectory with supplementary material"
    - "The old flat freeze.md and cherry-pick-prod.md files are deleted"
  artifacts:
    - path: "~/.claude/skills/freeze/SKILL.md"
      provides: "Directory lock skill with on-demand hook registration"
      contains: "on-demand"
    - path: "~/.claude/skills/freeze/references/hook-setup.md"
      provides: "Hook registration details extracted from SKILL.md for progressive disclosure"
    - path: "~/.claude/skills/cherry-pick-prod/SKILL.md"
      provides: "Worktree cherry-pick skill with trigger-focused description"
      contains: "Gotchas"
    - path: "~/.claude/skills/cherry-pick-prod/references/pr-template.md"
      provides: "PR template and workflow details for progressive disclosure"
  key_links:
    - from: "~/.claude/skills/freeze/SKILL.md"
      to: "~/.claude/skills/freeze/references/hook-setup.md"
      via: "Additional Resources section"
      pattern: "references/hook-setup.md"
    - from: "~/.claude/skills/cherry-pick-prod/SKILL.md"
      to: "~/.claude/skills/cherry-pick-prod/references/pr-template.md"
      via: "Additional Resources section"
      pattern: "references/pr-template.md"
---

<objective>
Upgrade freeze.md and cherry-pick-prod.md to SKILL.md directory structure with trigger-focused descriptions, Gotchas sections, and references/ subdirectories for progressive disclosure.

Purpose: These two skills have complex operational logic (hook registration, worktree management) that benefits from the SKILL.md pattern established in Phase 6. The upgrade improves Claude Code's ability to find and invoke these skills at the right moment, and documents failure modes explicitly.

Output: Two directory-based skills (freeze/, cherry-pick-prod/) replacing two flat .md files. Old flat files deleted.
</objective>

<execution_context>
@/Users/masa/.config/claude/get-shit-done/workflows/execute-plan.md
@/Users/masa/.config/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tps-kaizen-core-quality/06-01-SUMMARY.md

<interfaces>
<!-- Established SKILL.md pattern from Phase 6 and existing directory-based skills -->

SKILL.md structure:
```yaml
---
name: skill-name
description: Trigger-focused description. Use when: <keyword1>, <keyword2>...
---
```
Followed by:
1. # /skill-name heading
2. ## Usage (code block with invocation examples)
3. ## Workflow / Behavior sections (core content)
4. ## Gotchas (named patterns with explanatory paragraphs)
5. ## Additional Resources (links to references/ files for progressive disclosure)

Example from adversarial-review/SKILL.md:
```yaml
---
name: adversarial-review
description: Spawn a fresh-eyes subagent to critique code, then iterate fixes until findings degrade to nitpicks. Use after completing a feature or before shipping.
---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade freeze.md to freeze/ SKILL.md with on-demand hook pattern</name>
  <files>
    ~/.claude/skills/freeze/SKILL.md
    ~/.claude/skills/freeze/references/hook-setup.md
  </files>
  <action>
Create directory structure:
- mkdir -p ~/.claude/skills/freeze/references/

Create ~/.claude/skills/freeze/SKILL.md:
1. YAML frontmatter with:
   - name: freeze
   - description: "On-demand directory lock — blocks Edit/Write outside a frozen directory via PreToolUse hook. Use when: protecting files, preventing accidental edits, locking directory scope, isolating changes to one directory."
2. Keep the existing content from freeze.md but restructure:
   - Usage section (the /freeze commands — keep as-is, they're good)
   - Behavior section (when active rules — keep as-is)
   - Implementation section with the three subcommands (/freeze dir, /freeze off, /freeze status) — keep the bash code blocks
   - State File section (keep)
   - On-Demand Hook Registration section: Reframe the "First-Time Setup" section to emphasize the on-demand pattern — the hook is registered once and then activated/deactivated via state file. The hook itself is always registered but does nothing when state file is absent (zero overhead). This is the on-demand hook pattern: registration is permanent, activation is on-demand via state file toggle.
3. Gotchas section — keep all 5 existing gotchas but format them as named patterns with ### headings and explanatory paragraphs (matching Phase 6 pattern):
   - ### Session Restart After Registration (existing)
   - ### Symlink Resolution (existing)
   - ### State File Deletion Semantics (existing)
   - ### Self-Management Allowed (existing)
   - ### Freeze Directory Boundary (existing)
4. Additional Resources section pointing to references/hook-setup.md

Extract to references/hook-setup.md:
- The detailed hook registration JSON snippet
- The checking setup status bash script
- The "If not registered" step-by-step instructions
- This is supplementary material that a first-time reader doesn't need but can reference when needed

After creating both files, delete the old flat file:
```bash
rm ~/.claude/skills/freeze.md
```

IMPORTANT: The description must use the trigger-focused "Use when:" format established in Phase 6 for Claude Code invocation matching. Do NOT change the functional content — only restructure into the directory format.
  </action>
  <verify>
    <automated>test -f ~/.claude/skills/freeze/SKILL.md && test -f ~/.claude/skills/freeze/references/hook-setup.md && ! test -f ~/.claude/skills/freeze.md && grep -q "on-demand\|On-Demand\|on demand" ~/.claude/skills/freeze/SKILL.md && grep -q "Gotchas" ~/.claude/skills/freeze/SKILL.md && grep -q "references/hook-setup.md" ~/.claude/skills/freeze/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>freeze/ directory exists with SKILL.md (trigger-focused description, on-demand hook pattern documented, 5+ Gotchas with ### headings, Additional Resources pointing to references/), references/hook-setup.md exists with detailed hook registration instructions, old freeze.md is deleted</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade cherry-pick-prod.md to cherry-pick-prod/ SKILL.md with Gotchas and references/</name>
  <files>
    ~/.claude/skills/cherry-pick-prod/SKILL.md
    ~/.claude/skills/cherry-pick-prod/references/pr-template.md
  </files>
  <action>
Create directory structure:
- mkdir -p ~/.claude/skills/cherry-pick-prod/references/

Create ~/.claude/skills/cherry-pick-prod/SKILL.md:
1. YAML frontmatter with:
   - name: cherry-pick-prod
   - description: "Safely cherry-pick commits to a release/prod branch using an isolated git worktree. Use when: hotfix, backport, cherry-pick, production fix, release branch patch, emergency deploy."
2. Keep the existing content but restructure:
   - Usage section (the /cherry-pick-prod commands — keep as-is)
   - Workflow section with all 6 steps (Preflight, Create Worktree, Cherry-Pick, Verify, Push & PR, Cleanup) — keep as-is, this is the core value
3. Gotchas section — upgrade the existing 5 bullet-point gotchas to named patterns with ### headings and explanatory paragraphs:
   - ### Stale Target Branch — Always fetch before worktree creation because the worktree is based on the local branch ref, not remote
   - ### Force Push Prohibition — Never force-push the cherry-pick branch; if rebase needed, recreate the worktree from scratch because the worktree's history should be a clean fork
   - ### Mid-Sequence Abort — If cherry-pick fails partway through a multi-commit sequence, abort in the worktree before cleanup, otherwise the worktree is left in a dirty state
   - ### Worktree Cleanup on Failure — Worktree must be removed even on error; use trap or finally logic to prevent /tmp accumulation of orphaned worktrees
   - ### Merge Commit Parents — Don't cherry-pick merge commits without --mainline flag; ask user which parent since the tool cannot infer which side of the merge the user wants
4. Additional Resources section pointing to references/pr-template.md

Extract to references/pr-template.md:
- The PR template markdown block (the ## Cherry-pick to target-branch template)
- Extended PR body guidance (source commits listing, conflict resolution details, verification status)

After creating both files, delete the old flat file:
```bash
rm ~/.claude/skills/cherry-pick-prod.md
```

IMPORTANT: Use the trigger-focused "Use when:" description format. Do NOT change the functional workflow — only restructure.
  </action>
  <verify>
    <automated>test -f ~/.claude/skills/cherry-pick-prod/SKILL.md && test -f ~/.claude/skills/cherry-pick-prod/references/pr-template.md && ! test -f ~/.claude/skills/cherry-pick-prod.md && grep -q "Gotchas" ~/.claude/skills/cherry-pick-prod/SKILL.md && grep -q "references/pr-template.md" ~/.claude/skills/cherry-pick-prod/SKILL.md && grep -c "^### " ~/.claude/skills/cherry-pick-prod/SKILL.md | awk '{exit ($1 >= 5) ? 0 : 1}' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>cherry-pick-prod/ directory exists with SKILL.md (trigger-focused description, 5 named Gotchas with ### headings and explanatory paragraphs, Additional Resources section), references/pr-template.md exists with PR template details, old cherry-pick-prod.md is deleted</done>
</task>

</tasks>

<verification>
1. Both freeze/ and cherry-pick-prod/ directories exist under ~/.claude/skills/
2. Each has SKILL.md with YAML frontmatter (name, description with trigger keywords)
3. Each has Gotchas section with named ### patterns
4. Each has references/ subdirectory with at least one reference file
5. Each SKILL.md has Additional Resources section pointing to references/
6. Old flat .md files (freeze.md, cherry-pick-prod.md) are deleted
7. freeze/SKILL.md documents the on-demand hook registration pattern
</verification>

<success_criteria>
- freeze/SKILL.md exists with on-demand hook pattern, 5+ named Gotchas, references/ with hook-setup.md
- cherry-pick-prod/SKILL.md exists with trigger-focused description, 5 named Gotchas, references/ with pr-template.md
- Old flat files deleted
- All functional content preserved (no behavioral changes)
</success_criteria>

<output>
After completion, create `.planning/phases/09-standalone-skill-upgrades/09-01-SUMMARY.md`
</output>
